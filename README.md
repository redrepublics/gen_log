# Генерация тестового лога
*python gen.py --number 40 --step 100* 
* В базовом значении два аргумента
* * --number (по умолчани 30) -- колличество записей (шт.)
* * --step (по умолчанию 1) -- шаг по времени (сек.)
* файл лога создается в текущую папку